---
title: "The XZ backdoor"
description: "An interactive walkthrough of CVE-2024-3094 — how a two-year social-engineering campaign almost shipped a sshd backdoor in liblzma."
date: 2026-04-25
draft: true
categories: ["security"]
tags: ["xz", "cve-2024-3094", "supply-chain", "reverse-engineering"]
---

import '../../../styles/xz.css';
import Mnemonic from '~/components/xz/Mnemonic.astro';
import Timeline from '~/components/xz/Timeline';
import TarballDiff from '~/components/xz/TarballDiff.astro';
import PayloadChain from '~/components/xz/PayloadChain';
import DepGraph from '~/components/xz/DepGraph';
import LatencyChart from '~/components/xz/LatencyChart.astro';
import DistroGrid from '~/components/xz/DistroGrid.astro';

{/* §0 — Prelude (~150 words). Open with the surprising premise — a
    compression library backdoored sshd. Frame the question the rest of
    the post answers. */}

## Act I — Arrival

{/* ~600 words. Narrate the social-engineering arc. Cite Russ Cox
    xz-timeline. Quote the Jigar Kumar emails verbatim. End with Jia Tan
    signing 5.4.1 in Jan 2023. */}

<Timeline client:visible />

## Act II — The Trigger

{/* ~500 words. Explain autoconf for non-RE readers; introduce the
    tarball-vs-git distinction; flag the test-file trick. Cite Russ Cox
    xz-script. */}

<TarballDiff />

## Act III — The Payload

{/* ~700 words. Walk the reader through the chain. Use the widget as
    the spine — for each card, one or two sentences in prose explaining
    what's about to happen, then let the widget show the bytes. End at
    the ELF magic moment. */}

<PayloadChain client:load />

## Act IV — The Hijack

{/* ~800 words, RE-course weighted. Three subsections:
    1. The path — sshd → libsystemd → liblzma (sd_notify distro patch).
       Use the dep-graph below.
    2. The resolver — IFUNC mechanics. Why running before RELRO matters.
       Walk three textual disassembly snippets (extracted by
       scripts/extract-disasm.sh into xz-artifacts/analysis/disasm/):
         - _get_cpuid — IFUNC entry that pretends to return CPU info
         - The GOT walker — finds sshd's entry for RSA_public_decrypt
         - The hooked RSA_public_decrypt — parses [tag][ed448_sig][cmd]
       Use <Mnemonic op="…">…</Mnemonic> around AT&T mnemonics worth a
       click-through to AsmLookup.
    3. The gate — Ed448 signature verification. Reference amlweems/xzbot
       for the working PoC.
    Cite smx-smx/xzre for function naming. */}

<DepGraph client:visible />

{/* Disassembly snippets — the .s files live at
    xz-artifacts/analysis/disasm/. Decision pending: paste contents
    directly (one Expressive Code block each) or build a tiny
    <DisasmBlock src="get_cpuid.s"/> that imports them at build time. */}

<aside class="xz-tryit not-prose">
  <p class="xz-tryit-title">Try it yourself</p>
  <p>The artifacts referenced in this section live at <code>xz-artifacts/analysis/xzre/liblzma_la-crc64-fast.o</code> (see <code>xz-artifacts/README.md</code> for provenance and safety notes).</p>
  <ul>
    <li><code>objdump -d -M att xz-artifacts/analysis/xzre/liblzma_la-crc64-fast.o | grep -A 30 '&lt;_get_cpuid&gt;:'</code></li>
    <li>In <code>gdb</code>, set a breakpoint on <code>RSA_public_decrypt</code> and inspect the stack when an SSH client connects with a custom certificate.</li>
    <li><a href="https://github.com/amlweems/xzbot" target="_blank" rel="noopener noreferrer">amlweems/xzbot</a> has a working honeypot patch for OpenSSH and the attacker's Ed448 public key extracted.</li>
  </ul>
</aside>

## Act V — The Discovery

{/* ~400 words. Narrate the discovery — Andres Freund's accidental find
    while micro-benchmarking PostgreSQL. Quote his oss-security email
    opener verbatim. */}

<LatencyChart />

## Coda

{/* ~250 words. What shipped where, what was spared, what changed in
    the open-source ecosystem afterward. */}

<DistroGrid />

## Notes & sources

{/* ~150 words. Russ Cox–style citation list. Every claim above gets a
    primary-source link. Use the spec's reference set as the starting
    point. */}
